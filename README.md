# 剑指美加墨（Targeting 2026）

> 世界杯结束以后，哪支球队仍是你的意难平？选中它，重新征召、排兵布阵，并亲手改写那条没有走完的路。

《剑指美加墨》是一款面向 2026 世界杯主题的离线优先像素足球游戏。它把国家队经营、24 人征召、阵型与情报、赛前更衣室、约 3 分钟的 11v11 实时比赛、临场决策、换人、战术、点球大战、赛后复盘与图鉴收集串成一条完整征程。

当前正式内容范围固定为 **16 支可执教国家队**，不再以扩展到 48 支可玩队为交付目标。完整世界杯赛程仍保留 48 队对手数据，用于分组和淘汰赛路径。仓库只维护 `main` 上的一套业务源码，展示完整版、合规完整版和互动空间合规版都由同一提交派生，禁止手工维护版本副本。

## 一眼看懂当前版本

| 模块 | 当前实现 |
| --- | --- |
| 国家队 | 16 支可执教队，每队 24 名候选球员；各自拥有难度、预算、现实目标、战术画像、专属技能文案、委任书与结局语义 |
| 排兵布阵 | 10 套阵型、位置适配惩罚、阵容三维评分、阵型克制情报、手机端拖拽与比赛情报折叠 |
| 教练模式 | HappySeed 11v11 Runtime、22 人业务 actor 映射、约 3 分钟有效比赛、补时、加时与点球 |
| 场内决策 | 53 个导演场景、126 个选择、400 条 outcome 分支；结果与画面、比分、播报、统计共用事件源 |
| 临场调整 | 5 档战术姿态；换人最多 5 人、3 个窗口；体力、阵型站位与 Runtime 角色同步变化 |
| 更衣室 | 42 个赛前/中场/加时场景、88 个选项；士气与状态会进入成功率公式，雨天有专属预警 |
| 后勤与情报 | 医疗、营养、心理、数据、训练、球探 6 部门；直接属性收益和信息收益分开计算 |
| 天气 | 晴天与雨天两套现场表现；雨天具备像素雨、本地双层雨声、专属场景与选择风险 |
| 球员模式 | 同一 Runtime 上的训练基地、触摸摇杆、上下文动作键、键盘与手柄输入 |
| 收集 | 16 队最好成绩、冠军历史、征程记录、战术/替补/逆袭等 22 个成就定义与多级目标 |
| 复盘 | 本地统计驱动的教练建议与赛后复盘，不依赖联网，也不会暗改比赛结果 |

## 为什么它不是“一次性 Demo”

完整循环是：

1. 选择国家队，接受足协任务并签署专属聘书。
2. 从 24 人候选池征召阵容，处理预算、位置和状态取舍。
3. 投资后勤，阅读由真实对手数据计算出的情报。
4. 选择阵型并拖拽球员完成 11 人首发。
5. 在更衣室处理天气、士气、伤病、老将和点球准备等问题。
6. 进入持续运行的 11v11 比赛，用决策、战术和换人改变走势。
7. 赛后阅读基于本场统计和关键决策生成的复盘，把建议带入下一场。
8. 结算奖金、球队最好成绩、征程历史与图鉴成就，再挑战另一支“意难平”。

16 支球队不是同一套数值换皮。西班牙、法国、阿根廷等强队承担夺冠压力；挪威与加拿大以突破历史为目标；佛得角追求小组出线；库拉索争取世界杯首胜。难度、阵容深度、预算、目标、技能和叙事共同构成不同的执教命题。

完整策划书与机制算法已经合并为一份项目文档：

- [《剑指美加墨》游戏策划书（Markdown）](docs/剑指美加墨_游戏策划书.md)
- [《剑指美加墨》游戏策划书（Word）](docs/剑指美加墨_游戏策划书_2026-07-26.docx)

## 核心公平原则

- 足球运动、比分、触发和事实播报全部由本地 Runtime 决定。
- AI 不参与足球物理、比分结算或隐藏胜率；当前教练建议与赛后复盘使用本地确定性分析，断网可用。
- 数据中心、情报部门和赛后建议提供的是“可采取的行动”，不会偷偷给玩家加胜率；玩家采用推荐阵型、换人或点球方向后，才通过正式机制产生收益。
- 商业化道具接口仍为占位设计；不售卖直接进球、直接胜利、裁判偏置或决策概率。

## 可复现的平衡证据

项目内置 Monte Carlo 和语义审计脚本。2026-07-26 的当前基线包括：

- 16 队全对阵 72,000 场模拟，场均 6.60 次教练决策；错误阵容、是否换人和战术姿态会产生可见差异。
- 2,000 届同策略中立锦标赛，用于观察 16 队冠军分布，而不是手填“强队必夺冠”。
- 10,000 次完整征程模拟中，医疗、营养、心理和训练四类直接后勤都有正向贡献；四类全满的实验模型相对基线胜率提高 18.0 个百分点。
- 情报验证中，针对门将弱侧射门相对盲射提高约 8.8 个百分点；阵型克制建议和弱点选阵在总体样本中也为正收益。

这些数字是开发校准模型，不是对现实世界杯的预测；权威运行结果仍以正式 Runtime 和当前代码为准。

## 本地运行

要求 Node.js 20 或更高版本。

```bash
npm ci
npm run dev
```

固定开发入口：

| 命令 | 版本 | 端口 |
| --- | --- | --- |
| `npm run dev` / `npm run dev:showcase` | 展示完整版 | 5175 |
| `npm run dev:compliant` | 合规完整版；素材未齐时拒绝启动 | 5176 |
| `npm run dev:interactive` | 互动空间合规版；素材未齐时拒绝启动 | 5173 |

开发构建会显示版本标识，正式包不会显示。验收入口包括：

| 入口 | 用途 | 属性 |
| --- | --- | --- |
| `index.html` | 完整 React 游戏流程 | 正式入口 |
| `happyseed-runtime.html` | 教练/球员模式比赛 Runtime | 正式比赛 |
| `happyseed-decision-review.html` | 53 个场内决策逐项验收 | 开发工具 |
| `happyseed-runtime-lab.html` | Runtime、角色、球场和输入实验室 | 开发工具 |

## 验证命令

```bash
npm test -- --run
npm run lint
npm run audit:human-slice
npm run audit:kits
npm run audit:font-subset
npm run audit:runtime-actors
npm run audit:match-equipment
npm run audit:stadium-slice
npm run audit:match-sfx
npm run audit:decision-scenes
npm run build:showcase
npm run verify:compliant-pack
git diff --check
```

算法与情报可以单独复现：

```bash
node scripts/full-balance-sim.mjs --runs 300 --seed 20260726
node scripts/logistics-impact-sim.mjs --rounds 10000
node scripts/intel-validation-sim.mjs --rounds 3000
```

## 目录与资产约定

- `src/`：React 外壳、16 队数据、经营系统、比赛会话、决策、复盘与测试。
- `public/match-runtime-min/`：第三方物理核心与自有 HappySeed 适配层。
- `public/assets/`：两个美术包共用的界面、头像、聘书、事件、点球和本地音频。
- `art-packs/showcase/`：演示用原始标题、奖杯、48 面国旗和 16 个队徽。
- `art-packs/compliant/`：合规替换素材；未完整验收前保持 `pending`，构建不会回退到原图。
- `config/variants.mjs` 与 `config/art-rights.json`：三个构建目标、功能边界和版权资源清单的唯一来源。
- `public/pixel/`：生产像素球员、16 队球衣角色资源、装备和球场切片。
- `docs/`：游戏策划、机制算法、设计说明、验收与开发交接。
- 当前三版本与互动空间包体边界交接见 `docs/HANDOFF_2026-08-03_VARIANTS.md`。
- `scripts/`：资源生成、资产审计、包体构建与平衡模拟。

`public/match-runtime-min/scripts/match.rebuilt.js` 是第三方物理核心，不直接修改；自有适配逻辑位于 `public/match-runtime-min/standalone-match.js`、`public/match-runtime-min/happyseed/` 和 `src/services/happySeedMatchRuntime.js`。

## 发布与包体策略

```bash
npm run build:showcase
npm run build:compliant
npm run build:interactive
npm run release:all
```

- `release:all` 只允许在干净的 `main` 上执行，并从同一 Git SHA 构建三个目标及 `release-manifest.json`。
- 两个完整版只生成 `.variant-build/<variant-id>/` 网页目录，不生成 ZIP；它们用于本地演示、录制或后续网页部署。
- 唯一会进入 `artifacts/<YYYYMMDD>-<short-sha>/` 的上传包是 `targeting-2026-compliant-interactive.zip`，包内包含 `build-info.json`。
- 合规素材缺失、尺寸不符、与展示素材字节相同或最终包泄漏原图时，合规构建直接失败。
- 互动空间版固定四支可选队，保留 48 队赛程、正式比赛点球、教练/球员模式、缩放和完整比赛音效；移除图鉴与独立点球入口。
- 互动空间 ZIP 根目录必须直接包含 `index.html`，全部路径使用 ASCII，纯离线且不超过 8 MiB；正式出包必须通过互动空间 `h5-validator`。
- `.variant-build/`、`.variant-public/`、`artifacts/`、旧 `dist*`、`deliverables/`、`release/` 和 `reports/` 都是本地生成物，不提交 Git。

## 当前边界

- 16 支球队技能的设计数据合同已经齐全；逐队 Runtime 效果仍需按技能做独立 A/B 验收，文档不会把展示文案冒充已经验证的隐藏加成。
- 运动饮料、冰袋、球鞋、手套、战术板和训练器材属于公平商业化接口占位，尚未进入正式比分或胜率链路。
- 火山引擎 AI 目前不接入核心玩法。现有本地建议和复盘已经能够解释本场发生了什么，并保持断网可玩与结果可复现。

## 项目初心

我们不想做一款“换个队徽就结束”的世界杯小游戏。玩家选择的是一段真实足球语境里的遗憾：豪门为什么又差一步，黑马能不能证明上次不是偶然，新军能不能留下第一场胜利。游戏给出的不是预设答案，而是一套足够完整、足够透明的系统，让玩家用自己的名单、阵型、判断和临场选择重走来时路。

## 开源组件说明

比赛 Runtime 界面与部分基础运动、物理能力借鉴了 HappySeed 的开源组件，但本项目并未直接把原组件作为整套游戏使用。我们在其基础上对国家队数据映射、22 人 Runtime actor、像素球员与球衣、球场与球门球网、裁判规则、53 个临场决策场景、战术与换人、比分统计、补时与点球、天气音效、移动端交互以及 React 业务衔接进行了大量适配和改造。

HappySeed 相关能力被隔离在独立适配层中；页面、赛事、经营、存档、决策、复盘和收集系统均由本项目自行控制。因此项目对原组件的整体依赖度有限，边界清晰，也为后续替换或继续演进保留了空间。
